# Minimal Netlify configuration for Next.js
# Netlify auto-detects Next.js and uses OpenNext adapter

[build]
  command = "npm run build"

# Environment settings  
[build.environment]
  NODE_VERSION = "20"
  NEXT_TELEMETRY_DISABLED = "1"